{
    "version": 2,
    "builds": [
        {
            "src": "app.py",
            "use": "@vercel/python",
            "config": {
                "buildCommand": "pip install -r requirements.txt && PRISMA_PY_BINARY_CACHE_DIR=./.prisma_engines prisma generate && PRISMA_PY_BINARY_CACHE_DIR=./.prisma_engines prisma py fetch"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "app.py"
        }
    ]
}